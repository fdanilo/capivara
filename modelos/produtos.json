produtos :{
    _id,
    cod_barra,
    nome,
    descricao,
    marca,
    categoria: [{
      id_categoria
    }],

    subcategoria: [{
        id_subcategoria
    }],
    validade,
    data_cadastro,

    ingredientes: [{
        id_ingrediente
    }],
    colabs: [{
        tipo_colab,
        usr_id
    }],
    id_marca,
}
